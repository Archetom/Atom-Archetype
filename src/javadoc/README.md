# Atom Archetype documentation

This artifact is a Maven archetype, not a Java API library, so it has no Java
packages from which conventional Javadoc can be generated. The project and the
generated architecture are documented in the README and `docs/` resources in
the main and sources artifacts:

https://github.com/Archetom/atom-archetype
